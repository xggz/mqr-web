const config = {
    // 基础url地址
    baseUrl: '/api',
    // 接口请求的超时 时间
    timeout: 60000,
    // 接口通用请求头
    headers: {
        'Access-Token': '',
        'Content-Type': 'application/json'
    }
}

export default config;